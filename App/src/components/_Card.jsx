import styled from "styled-components";

const Card = styled.div`
  background-color: #ffffff;
  border-radius: var(--corner-md);
  padding: var(--corner-lg);

  box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
`;

export default Card;
